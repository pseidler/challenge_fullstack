/**
 * When a user clicks on a card, we want to show a modal with similar items.
 *
 * Use the same API as the main search to retrieve similar items. It's up to you
 * to decide what "similar" means.
 *
 * A user should be able to close the modal.
 *
 * You're encouraged to reuse as much code as possible.
 *
 */

export const SimilaritySearch = () => {
  return null;
};
